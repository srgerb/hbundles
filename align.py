#Aligns chains from a pdb file individually
#takes in 2 arguments, the base pdb to align to and the pdb to align
#seperates chains and outputs as temp pdb's (base off of pdb2 name)
#TMAlign each chain individually to base
#get TMSup?
#
